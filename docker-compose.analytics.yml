# GENERATED by `uv run analytics/compose_gen.py` from analytics/cameras.yml.
# Do not edit: run the generator. `--check` fails when this is out of date.
#
# One Savant source adapter per camera. They read RTSP from MediaMTX rather
# than the mp4 files directly - reading the files would be cheaper and would
# make browser overlays permanently impossible, because Savant and MediaMTX
# would each loop the same recording independently and drift apart within
# minutes. Both have to watch the same decoded stream.

services:
  savant-source-03:
    image: ghcr.io/insight-platform/savant-adapters-gstreamer:latest
    entrypoint: /opt/savant/adapters/gst/sources/rtsp.sh
    environment:
      RTSP_URI: 'rtsp://cameras:8554/cam3'
      RTSP_TRANSPORT: 'tcp'
      SOURCE_ID: '03'
      ZMQ_ENDPOINT: 'dealer+connect:tcp://savant-module:5555'
      USE_ABSOLUTE_TIMESTAMPS: 'True'
      STALL_ACTION: 'fail'
      LOGLEVEL: 'info'
    depends_on:
      savant-module:
        condition: service_healthy
      cameras:
        condition: service_started
    restart: unless-stopped
  savant-source-04:
    image: ghcr.io/insight-platform/savant-adapters-gstreamer:latest
    entrypoint: /opt/savant/adapters/gst/sources/rtsp.sh
    environment:
      RTSP_URI: 'rtsp://cameras:8554/cam4'
      RTSP_TRANSPORT: 'tcp'
      SOURCE_ID: '04'
      ZMQ_ENDPOINT: 'dealer+connect:tcp://savant-module:5555'
      USE_ABSOLUTE_TIMESTAMPS: 'True'
      STALL_ACTION: 'fail'
      LOGLEVEL: 'info'
    depends_on:
      savant-module:
        condition: service_healthy
      cameras:
        condition: service_started
    restart: unless-stopped
  savant-source-05:
    image: ghcr.io/insight-platform/savant-adapters-gstreamer:latest
    entrypoint: /opt/savant/adapters/gst/sources/rtsp.sh
    environment:
      RTSP_URI: 'rtsp://cameras:8554/cam5'
      RTSP_TRANSPORT: 'tcp'
      SOURCE_ID: '05'
      ZMQ_ENDPOINT: 'dealer+connect:tcp://savant-module:5555'
      USE_ABSOLUTE_TIMESTAMPS: 'True'
      STALL_ACTION: 'fail'
      LOGLEVEL: 'info'
    depends_on:
      savant-module:
        condition: service_healthy
      cameras:
        condition: service_started
    restart: unless-stopped
  savant-source-06:
    image: ghcr.io/insight-platform/savant-adapters-gstreamer:latest
    entrypoint: /opt/savant/adapters/gst/sources/rtsp.sh
    environment:
      RTSP_URI: 'rtsp://cameras:8554/cam6'
      RTSP_TRANSPORT: 'tcp'
      SOURCE_ID: '06'
      ZMQ_ENDPOINT: 'dealer+connect:tcp://savant-module:5555'
      USE_ABSOLUTE_TIMESTAMPS: 'True'
      STALL_ACTION: 'fail'
      LOGLEVEL: 'info'
    depends_on:
      savant-module:
        condition: service_healthy
      cameras:
        condition: service_started
    restart: unless-stopped
  savant-source-09:
    image: ghcr.io/insight-platform/savant-adapters-gstreamer:latest
    entrypoint: /opt/savant/adapters/gst/sources/rtsp.sh
    environment:
      RTSP_URI: 'rtsp://cameras:8554/cam9'
      RTSP_TRANSPORT: 'tcp'
      SOURCE_ID: '09'
      ZMQ_ENDPOINT: 'dealer+connect:tcp://savant-module:5555'
      USE_ABSOLUTE_TIMESTAMPS: 'True'
      STALL_ACTION: 'fail'
      LOGLEVEL: 'info'
    depends_on:
      savant-module:
        condition: service_healthy
      cameras:
        condition: service_started
    restart: unless-stopped
  savant-source-10:
    image: ghcr.io/insight-platform/savant-adapters-gstreamer:latest
    entrypoint: /opt/savant/adapters/gst/sources/rtsp.sh
    environment:
      RTSP_URI: 'rtsp://cameras:8554/cam10'
      RTSP_TRANSPORT: 'tcp'
      SOURCE_ID: '10'
      ZMQ_ENDPOINT: 'dealer+connect:tcp://savant-module:5555'
      USE_ABSOLUTE_TIMESTAMPS: 'True'
      STALL_ACTION: 'fail'
      LOGLEVEL: 'info'
    depends_on:
      savant-module:
        condition: service_healthy
      cameras:
        condition: service_started
    restart: unless-stopped
  savant-source-11:
    image: ghcr.io/insight-platform/savant-adapters-gstreamer:latest
    entrypoint: /opt/savant/adapters/gst/sources/rtsp.sh
    environment:
      RTSP_URI: 'rtsp://cameras:8554/cam11'
      RTSP_TRANSPORT: 'tcp'
      SOURCE_ID: '11'
      ZMQ_ENDPOINT: 'dealer+connect:tcp://savant-module:5555'
      USE_ABSOLUTE_TIMESTAMPS: 'True'
      STALL_ACTION: 'fail'
      LOGLEVEL: 'info'
    depends_on:
      savant-module:
        condition: service_healthy
      cameras:
        condition: service_started
    restart: unless-stopped
  savant-source-12:
    image: ghcr.io/insight-platform/savant-adapters-gstreamer:latest
    entrypoint: /opt/savant/adapters/gst/sources/rtsp.sh
    environment:
      RTSP_URI: 'rtsp://cameras:8554/cam12'
      RTSP_TRANSPORT: 'tcp'
      SOURCE_ID: '12'
      ZMQ_ENDPOINT: 'dealer+connect:tcp://savant-module:5555'
      USE_ABSOLUTE_TIMESTAMPS: 'True'
      STALL_ACTION: 'fail'
      LOGLEVEL: 'info'
    depends_on:
      savant-module:
        condition: service_healthy
      cameras:
        condition: service_started
    restart: unless-stopped
  savant-source-13:
    image: ghcr.io/insight-platform/savant-adapters-gstreamer:latest
    entrypoint: /opt/savant/adapters/gst/sources/rtsp.sh
    environment:
      RTSP_URI: 'rtsp://cameras:8554/cam13'
      RTSP_TRANSPORT: 'tcp'
      SOURCE_ID: '13'
      ZMQ_ENDPOINT: 'dealer+connect:tcp://savant-module:5555'
      USE_ABSOLUTE_TIMESTAMPS: 'True'
      STALL_ACTION: 'fail'
      LOGLEVEL: 'info'
    depends_on:
      savant-module:
        condition: service_healthy
      cameras:
        condition: service_started
    restart: unless-stopped
  savant-source-15:
    image: ghcr.io/insight-platform/savant-adapters-gstreamer:latest
    entrypoint: /opt/savant/adapters/gst/sources/rtsp.sh
    environment:
      RTSP_URI: 'rtsp://cameras:8554/cam15'
      RTSP_TRANSPORT: 'tcp'
      SOURCE_ID: '15'
      ZMQ_ENDPOINT: 'dealer+connect:tcp://savant-module:5555'
      USE_ABSOLUTE_TIMESTAMPS: 'True'
      STALL_ACTION: 'fail'
      LOGLEVEL: 'info'
    depends_on:
      savant-module:
        condition: service_healthy
      cameras:
        condition: service_started
    restart: unless-stopped
