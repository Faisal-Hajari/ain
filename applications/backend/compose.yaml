services:
  backend:
    build: .
    image: ain-backend:dev
    ports:
      - "8000:8000"
    environment:
      # The dashboard dev server; widen or replace per environment.
      AIN_CORS_ORIGINS: "http://localhost:5173,http://localhost:3000"
    restart: unless-stopped
